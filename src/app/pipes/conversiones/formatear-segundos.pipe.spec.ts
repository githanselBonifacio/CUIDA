import { FormatearSegundosPipe } from './formatear-segundos.pipe';

describe('FormatearSegundosPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearSegundosPipe();
    expect(pipe).toBeTruthy();
  });
});
